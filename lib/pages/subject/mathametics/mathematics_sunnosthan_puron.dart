import 'package:flutter/material.dart';

class MathSunnosthanPuron extends StatelessWidget {
  const MathSunnosthanPuron({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('শূন্যস্থান পূরণ'),
      ),
      body: Column(
        children: [Text('data')],
      ),
    );
  }
}
