import 'package:flutter/material.dart';

class MathSajiyeLekha extends StatelessWidget {
  const MathSajiyeLekha({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('সাজিয়ে লেখা'),
      ),
      body: Column(
        children: [Text('data')],
      ),
    );
  }
}
